/**
 * Simple enum named `SimpleEnum`
 *
 * @remarks Just an example of `Enum` docs generation
 *
 * @example
 * ```typescript
 * if(a === SimpleEnum.Hello) {}
 * ```
 */
enum SimpleEnum {
  /**
   * Summary comment to Hello member
   */
  Hello = "Hello",
}

export { SimpleEnum };
